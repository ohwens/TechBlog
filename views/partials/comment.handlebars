 {{#each this}}
  <section class="comment card" style="width: 18rem;">
    <div class = "body">
      <div class="meta">
        {{user.username}} on {{format_date createdAt}}:
      </div>
      <div class="text">
        {{comment_text}}
      </div>
    </div>
  </section>
  {{/each}}
</div>